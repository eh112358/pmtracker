services:
  pmtracker:
    build: .
    container_name: pmtracker
    ports:
      - "3000:8000"
    volumes:
      - ./backend/data:/app/data
    environment:
      - DATABASE_URL=sqlite:///./data/pmtracker.db
      - GOLDAPI_KEY=${GOLDAPI_KEY:-}
    restart: unless-stopped
