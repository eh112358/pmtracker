services:
  backend:
    build: ./backend
    container_name: pmtracker-backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend/data:/app/data
    environment:
      - DATABASE_URL=sqlite:///./data/pmtracker.db
      - GOLDAPI_KEY=${GOLDAPI_KEY:-}
    restart: unless-stopped
    networks:
      - pmtracker-net

  frontend:
    build: ./frontend
    container_name: pmtracker-frontend
    ports:
      - "3000:8080"
    depends_on:
      - backend
    restart: unless-stopped
    networks:
      - pmtracker-net

networks:
  pmtracker-net:
    driver: bridge
